FROM node:20

WORKDIR /app

# Copy package files first
COPY package*.json ./

# Install dependencies ignoring peer conflicts
RUN npm install --legacy-peer-deps

# Copy source code
COPY . .

# Build using npx so it finds local svelte-kit
RUN npm run build

EXPOSE 4000

CMD ["node", "build/index.js"]
